import { renderHook, act } from '@testing-library/react';
import { useData } from '../useData';

describe('useData Hook', () => {
  it('should handle fetching state correctly', () => {
    expect(true).toBe(true);
  });
});
